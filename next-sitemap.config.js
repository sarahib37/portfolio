/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://yourdomain.com', // Change this to your actual domain
    generateRobotsTxt: true, // Generates both sitemap.xml & robots.txt
  };
  